const Header = () => (
  <header className="row">
    <div className="col">
      <h1 className="text-secondary">todos</h1>
      <p className="text-secondary">Items will persist in the local storage</p>
    </div>
  </header>
);

export default Header;
